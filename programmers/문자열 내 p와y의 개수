#include <string>
#include <iostream>
using namespace std;

bool solution(string s)
{
    int p = 0;
    int y = 0;
    for(int i = 0; i < s.length(); i++){
        if('p' == tolower(s[i]))
            first++;
        else if('y' == tolower(s[i]))
            end++;
    }
    return p == y;
}
